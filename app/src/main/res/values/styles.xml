<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BottomMenuStyle" parent="@style/Widget.MaterialComponents.BottomNavigationView">
        <item name="itemTextColor">@color/red</item>
        <item name="itemIconTint">@color/selector_menu_item_icon_tint</item>
        <item name="itemRippleColor">#30ff0000</item>
    </style>
    <style name="MyCheckBox" parent="Widget.MaterialComponents.CompoundButton.CheckBox">
        <item name="buttonTint">@color/red</item>
    </style>
</resources>
